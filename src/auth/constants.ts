// It's highly recommended to move this to an environment variable (.env file) for production
// but for now, this ensures consistency.
export const jwtConstants = {
    secret: 'gourmetProSecretKey2024', // This is now the single source of truth
};

